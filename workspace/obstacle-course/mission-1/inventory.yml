---
# The SSH key path comes from ansible.cfg (in this directory and the
# project root), so commands work whether you cd here or run from root.

all:
  children:
    wht_ssh:
      hosts:
        wht-ssh:
          ansible_host: localhost
          ansible_port: 2241
          ansible_user: cadet
